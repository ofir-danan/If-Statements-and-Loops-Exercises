function question10() { 
    let i;
    let j;
    let k;
    for(x = 1; x <= 6; x++){
        for (y = 1; y < x; y++) {
        k = k + ("*");        
        }
        console.log(chr);
        chr='';    
    }
}